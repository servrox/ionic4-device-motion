language: node_js
node_js:
  - stable

branches:
  only:
  - master
  - develop

install:
  - npm install
  - npm install -g ionic

script:
  - ionic build --prod

after_script:
  - firebase deploy --token $FIREBASE_TOKEN